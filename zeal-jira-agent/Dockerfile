FROM python:3.12-slim

WORKDIR /app

COPY zeal-jira-agent/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY zeal-jira-agent/ .

EXPOSE 8000

CMD ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
